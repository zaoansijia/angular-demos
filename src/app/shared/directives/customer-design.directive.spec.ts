import { CustomerDesignDirective } from './customer-design.directive';

describe('CustomerDesignDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerDesignDirective();
    expect(directive).toBeTruthy();
  });
});
